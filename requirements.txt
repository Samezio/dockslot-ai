# The deterministic layer (app/db.py, app/models.py, app/repository.py)
# is stdlib-only. Everything below is for the conversational layer
# (app/llm.py, app/intent.py, app/conversation.py).
#
# langchain gives us one interface across LLM providers (see app/llm.py) --
# swapping LLM_PROVIDER in .env is a config change, not a code change.
# langchain-google-genai backs the default provider (Gemini);
# langchain-openai backs both "openai" and "openrouter" (OpenRouter is
# OpenAI-wire-compatible, see app/llm.py's _PROVIDERS dict).
langchain==0.3.30
langchain-google-genai==2.1.12
langchain-openai==0.3.35
pydantic==2.13.4
python-dotenv==1.2.1

# Local dev only -- backs the "ollama" provider in app/llm.py (no API key,
# no cost, runs against a local Ollama server). Not needed for
# google_genai/openai/openrouter.
langchain-ollama==0.3.10

# Optional facility-wide scheduling engine (app/scheduling.py, brief
# section 7.3). Used offline/deterministically -- no network calls.
ortools==9.15.6755

# REST API + UI (app/api.py, web/index.html). CLAUDE.md section 13
# names FastAPI as the default for REST APIs.
fastapi==0.128.8
uvicorn==0.39.0
